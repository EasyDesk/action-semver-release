name: Composite Actions CI

on:
  push:
    tags:
      - "*"

concurrency: new-tag
jobs:
  release:
    name: GitHub Release Job
    runs-on: ubuntu-20.04
    steps:
      - id: semver-checkout
        name: Checkout
        uses: EasyDesk/action-semver-checkout@master
      - name: Release
        if: ${{ steps.semver-checkout.outputs.should-release == 'true' }}
        uses: ./
        with:
          version: ${{ steps.semver-checkout.outputs.version }}
          prerelease: ${{ steps.semver-checkout.outputs.is-github-prerelease }}
          